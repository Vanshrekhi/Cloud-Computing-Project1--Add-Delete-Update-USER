# Backend

Express REST API for the application tier. Copy `.env.example` to `.env`, set `MONGODB_URI` and `FRONTEND_URL`, then run `npm install` and `npm run dev`.

For Render: set build command `npm install`, start command `npm start`, and configure `MONGODB_URI` plus the deployed Netlify URL as `FRONTEND_URL`.
